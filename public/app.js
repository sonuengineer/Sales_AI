const app=document.querySelector('#app'),roleNames={ADMIN:'Admin',INSTRUCTOR:'Instructor',STUDENT:'Student'},navigation={ADMIN:['Dashboard','Users','Courses','Cohorts','Reports','Certificates','Settings'],INSTRUCTOR:['Dashboard','Courses','Cohorts','CRM Lab','Assignments','Quizzes','Capstones','Profile'],STUDENT:['My Learning','CRM Lab','Dashboard','Assignments','AI Practice Lab','Capstone','Profile']};
async function api(path,options={}){const response=await fetch(path,{headers:{'Content-Type':'application/json'},...options}),data=await response.json();if(!response.ok)throw new Error(data.error||'Something went wrong.');return data}
function loginView(message=''){app.innerHTML=`<main class="auth"><section class="card"><div class="eyebrow">Fictional training environment</div><h1>Welcome back</h1><p>Sign in to your Sales Intelligence learning workspace.</p><form id="login-form"><label>Email<input name="email" type="email" required autocomplete="email"></label><label>Password<input name="password" type="password" required autocomplete="current-password"></label><button type="submit" style="margin-top:20px;width:100%">Sign in</button></form>${message?`<div class="error">${message}</div>`:''}<div class="demo"><strong>Local demo accounts</strong><br>admin@nexaflow.demo · instructor@nexaflow.demo · student@nexaflow.demo<br>Password: <code>demo123</code></div></section></main>`;document.querySelector('#login-form').addEventListener('submit',async event=>{event.preventDefault();try{await api('/api/login',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))});render()}catch(error){loginView(error.message)}})}
async function dashboardView(user){try{const {dashboard}=await api(`/api/dashboard/${user.role.toLowerCase()}`),nav=navigation[user.role].map((item,index)=>`<span class="nav-item ${index===0?'active':''}">${item}</span>`).join(''),cards=dashboard.metrics.map(([label,value])=>`<article class="metric"><span>${label}</span><strong>${value}</strong></article>`).join('');app.innerHTML=`<div class="shell"><aside class="sidebar"><div class="brand">NexaFlow <small>Training platform</small></div><nav class="nav">${nav}</nav></aside><div class="content"><header class="topbar"><span class="eyebrow">${roleNames[user.role]} workspace</span><div class="profile"><span class="avatar">${user.name[0]}</span><span>${user.name}</span><button id="logout" class="secondary">Sign out</button></div></header><main class="main"><section class="page-header"><div><h1>${dashboard.heading}</h1><p>${dashboard.description}</p></div></section><section class="grid">${cards}</section><section class="state"><strong>Your Phase 1 workspace is ready.</strong><p>Course management, learning content and CRM tools will be added in their dedicated phases.</p></section><p class="notice">All NexaFlow records and results in this environment are fictional demo data.</p></main></div></div>`;document.querySelector('#logout').addEventListener('click',async()=>{await api('/api/logout',{method:'POST'});render()})}catch(error){app.innerHTML=`<main class="auth"><section class="card"><h1>We couldn’t load your workspace</h1><p>${error.message}</p><button onclick="location.reload()">Try again</button></section></main>`}}
async function render(){app.innerHTML='<div class="loading">Loading your workspace…</div>';try{const {user}=await api('/api/session');user?dashboardView(user):loginView()}catch{loginView('The service is unavailable. Please try again.')}}render();
